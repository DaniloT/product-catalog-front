import React from 'react';
import { SpinnerWrapper, SpinnerCircle } from '../styles/spinnerStyles';

const Spinner: React.FC = () => (
    <SpinnerWrapper>
        <SpinnerCircle />
    </SpinnerWrapper>
);

export default Spinner;
